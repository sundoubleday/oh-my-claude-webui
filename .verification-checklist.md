# 启动验证清单

## 预启动状态 ✅

- [x] 依赖安装完成 (440 packages)
- [x] PostCSS 配置修复 (CommonJS 格式)
- [x] 前端页面完整 (8个页面)
- [x] 后端路由完整 (5个 API + 1个 WebSocket)
- [x] Git 状态干净 (仅 postcss.config.js 待提交)

## 启动命令

```bash
cd "E:\Vibe Coding\Open Code\project\oh-my-claude-webui"
bun run dev
```

## 预期成功输出

```
[0] web dev:    ▲ Next.js 15.1.0
[0] web dev:    - Local:        http://localhost:3000
[0] web dev:  ✓ Ready in 2-5s
[1] server dev: Started development server: http://localhost:5757
```

## 验证步骤

### 1️⃣ 健康检查
```bash
# Backend health check
curl http://localhost:5757/health
# 预期: {"status":"ok"}
```

### 2️⃣ 前端访问测试

浏览器打开以下页面并验证:

| 页面 | URL | 验证点 |
|------|-----|--------|
| 首页 | http://localhost:3000 | 显示"oh-my-claude-webui"标题,有测试按钮和输入框 |
| 设置 | http://localhost:3000/settings | JSON编辑器,显示 Claude 配置 |
| MCP | http://localhost:3000/mcp | MCP服务器列表 |
| Skills | http://localhost:3000/skills | Skills列表 |
| Plugins | http://localhost:3000/plugins | Plugins列表 |
| History | http://localhost:3000/history | 历史对话记录 |
| Commands | http://localhost:3000/commands | 自定义命令编辑器 |
| Chat | http://localhost:3000/chat | 聊天界面 |

### 3️⃣ API 端点测试

```bash
# MCP 配置
curl http://localhost:5757/api/mcp/config

# Transcripts 列表
curl http://localhost:5757/api/transcripts

# Skills 列表
curl http://localhost:5757/api/skills

# Commands 列表
curl http://localhost:5757/api/commands

# Plugins 配置
curl http://localhost:5757/api/plugins
```

### 4️⃣ 样式验证

检查首页:
- Sidebar 是否正常显示
- 深色/浅色主题切换是否生效
- Tailwind 样式是否正确应用
- Shadcn/ui 组件是否正常渲染

### 5️⃣ 控制台检查

浏览器开发者工具 (F12):
- Console: 无错误
- Network: 所有请求返回 200
- WebSocket: /ws/chat 连接正常 (在 Chat 页面)

## 常见问题处理

### PostCSS 错误仍然出现
```bash
# 清除 Next.js 缓存
rm -rf apps/web/.next
bun run dev
```

### 端口冲突
```bash
# 查找占用端口的进程
netstat -ano | findstr :3000
netstat -ano | findstr :5757

# 杀掉进程或修改端口
# 修改: apps/server/src/index.ts (port: 5757)
# 修改: apps/web/next.config.js (如需改前端端口)
```

### 模块解析错误
```bash
# 重新安装依赖
rm -rf node_modules
bun install
```

## 提交代码

验证成功后执行:

```bash
git add apps/web/postcss.config.js
git commit -m "fix(web): change PostCSS config to CommonJS format for Next.js 15 compatibility"
```

---

**状态**: 等待用户执行 `bun run dev`
